import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Global shortcut that shows or hides the menu bar popup. No default: empty until the user records one.
    static let togglePopup = Self("togglePopup")
}
