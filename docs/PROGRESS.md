# Implementation progress log

Plan: `docs/superpowers/plans/2026-09-18-timetug-core-app.md` (12 tasks, subagent-driven).
Branch: `feature/core-app`. To resume: read this file and `git log`, then continue at the first task not marked complete.

| Task | Status | Commits |
|---|---|---|
| 1 Scaffold + Core model | complete, review clean | fbcfdb2..37c62e8 |
| 2 ConferenceLinkDetector | complete, review clean + hardening fix | 86eda30..85ee751 |
| 3 TakeoverPolicy | complete, review clean | a92ec5e..749d9c9 |
| 4 Ledger, Scheduler, TakeoverRequest | complete (DeepSeek Critical was a false positive, verified: no typo, 41/41 pass) | 7413e39..e3bf990 |
| 5 CalendarStore | complete (+ dedupe-merge fix so opt-in/links survive; re-review Importants were about the controller-ordered fix, adjudicated not defects) | 461ca02..cb44e8c |
| 6 DayAgenda | complete, review clean (Core package done: 63 tests) | 3e61801..f656bd1 |
| 7 EventKitSource | complete, review clean (build-only verification) | 8ee011c..399524c |
| 8 macOS shell + coordinator | complete (build + 8 app tests verified by controller; GUI checks pending user) | 4a3bf7a..a920ac8 |
| 9 Dropdown popover | complete, review clean (GUI check pending user) | c017000..f484b0a |
| 10 Takeover overlay | complete (build + 8 tests verified by controller; GUI checks pending user; DeepSeek Important on fireTest Join adjudicated not a defect, plan wording fixed) | cb9c4c5..d292e70 |
| 11 Settings window | complete (build + 8 tests verified; GUI checks pending user) | 0284da9..c47a1cd |
| 12 Docs, ADRs, manual checklist | complete (controller spot-checked ADR 0004 vs code and fixed one inaccuracy) | d332148..baff5d9 |
| 13 Brand assets + README | complete, review clean (GUI check pending user) | 06a64e7..46e175f |

## Notes
- 2026-09-18: brand artwork added by the user (`artwork/`, `Apps/macOS/Resources/Assets.xcassets`, `docs/ARTWORK_USAGE.md`); plan Task 13 covers wiring it in plus README. `artwork/Source` boards and menu bar PNG exports were intentionally not committed (still in ~/Downloads/TimeTugAssets).
- Repo is to be published as a public GitHub project. No push has been done; no LICENSE chosen yet (ask the user).
- DeepSeek is used for task reviews via a Haiku relay subagent (no secrets ever sent). Implementers run on Claude.
- Minor findings (for final review): `CalendarSource.displayName` is source metadata, arguably a display string in Core; `CalendarEvent.id` omits calendarID (collision speculative); test helpers force-unwrap.
- Task 2 leftover minors: Google redirect unwrap ignores bare google.com and /url/; regex rebuilt per call (perf).
- Reviewer calibration: DeepSeek flash hallucinated a test typo in Task 4 (verified false). Verify any Critical/Important finding against the repo before acting.
- Design change in Task 5: CalendarEvent.additionalCalendarKeys/allCalendarKeys; TakeoverPolicy opt-in passes if ANY calendar of a merged meeting is opted in; DayAgenda (Task 6) hides a merged meeting only if all its calendars are hidden (plan already updated).
- Task 5 minors: sourceNames uniqueKeysWithValues traps on duplicate source ids; dedupe key uses raw doubles. Task 6 minors: force-unwrapped day arithmetic in DayAgenda.make.
- Manual/visual verification steps (Tasks 8-11, checklist in docs/manual-tests) cannot be done by subagents: they build and run unit tests only; the user must do the manual checks.
- Task 8: plan test inputs 300s->299s (compact(300) is "5m"); fireTest() is added in Task 10 not Task 8; DeepSeek flagged both, adjudicated not defects.
- RESOLVED (user chose to fix; fixed in e4d7a09) (plan-mandated finding, Task 11): SettingsView launch-at-login toggle uses `try?` on SMAppService register/unregister, so failures are swallowed and the toggle can show a state that is not real. Suggested fix: on failure, revert the toggle to `SMAppService.mainApp.status == .enabled` and show a short message. Needs user decision (plan text mandates the current code).
- All 13 tasks done. Final whole-branch review (deepseek-v4-pro) in progress; result in .superpowers/sdd/final-review.md. Then: user to run docs/manual-tests/macos-checklist.md, decide the open question above, choose a LICENSE, and say whether to push to GitHub.
- Final whole-branch review (deepseek-v4-pro): no Critical; only Important = the launch-at-login toggle (same as the open question). Minors echoed: Google redirect unwrap gaps, CalendarEvent.id omits calendarID. Note: the review was thin (mostly restated known items), so a human/other-model pass on scheduling and timers is still worthwhile.
- 2026-09-18: launch-at-login fix done (e4d7a09); MIT license added (c5008a5); user chose local merge to master. Remaining for the user: run docs/manual-tests/macos-checklist.md, add a GitHub remote and push, decide artwork licensing.
- 2026-09-18 post-release changes: Settings window now comes to front (f8a2472); new `skipAllDayEvents` takeover setting, default on, with backward-compatible decoding of saved settings (a789980). Merged to master locally; nothing pushed.
- 2026-09-18: settings polish (3063319): menu bar next-meeting mode shows "No Meetings" when nothing is upcoming; Menu bar section now above Calendars; Takeover and Show-in-list are coupled in Core (takeover implies shown; hiding removes takeover; legacy conflicts resolve to takeover wins). Merged to master locally.
- 2026-09-18: list polish: dropdown has more padding; `skipAllDayEvents` (default on) hides all-day events from the day list only, in the Calendars section of Settings; all-day events NEVER take over (policy unconditional). Earlier a789980 had made all-day takeover conditional on the setting, which was a misreading and is corrected. Merged to master locally.
- 2026-09-18: Settings redesign step 1 (design critique rec 1): General and Calendars tabs, calendar list in its own scrolling region, resizable window with saved frame, first-run opens Calendars tab. Visual layout UNVERIFIED (no GUI in subagents). Remaining critique items: (2) disambiguate duplicate calendar names by account + group by account (needs CalendarInfo.accountName from EventKitSource), (3) table-style column headers + per-row accessibility labels + stronger checkbox contrast, (4) lead-time picker, consistent toggle wording, video-link warning, rename Menu bar section/General wording. Merged to master locally.
- 2026-09-18: Settings now a System Settings style NavigationSplitView (e1c4e72): sidebar panes Takeover / Calendars / Menu Bar / General, search field in sidebar with tested matching (SettingsSearch.swift, 12 tests) that also matches calendar names; choosing a result switches pane and highlights the row; first-run opens Calendars; window frame autosave name is now "TimeTugSettings.v2". Visual layout UNVERIFIED (no GUI in subagents): check .searchable in sidebar, No results placeholder, highlight padding, scroll-to-calendar, column widths. Merged to master locally. Remaining critique items: account disambiguation + grouping (needs CalendarInfo.accountName), column-header table + accessibility labels, Takeover wording/lead-time picker/video-link warning.
- 2026-09-18: Settings panes now General / Calendars / Tug Rules (2ea3eb0). Menu Bar merged into General (sections Menu bar + App); About sheet (logo lockup, version 0.1.0 build 1, tagline, MIT line); UI term "Takeover" renamed "Tug" everywhere in UI text (pane Tug Rules, checkbox Tug, Test tug); "takeover" kept as a hidden search keyword; internal code names unchanged. LogoLockup imageset added. App tests: 27; Core: 73. Visual layout UNVERIFIED. Merged to master locally.
- 2026-09-18: General pane order is now App then Menu bar (last). About sheet redesigned with puppy branding (cream light look in both appearances, hero crop from ~/Downloads/TimeTugAssets/Source/timetug-regenerated-icon-source-sheet.png top panel: crop 928x445 at offset Y=12 X=592, stored as AboutHero imageset and artwork/Branding/timetug-about-hero*.png; lockup, tagline pill, version, MIT, Done). The Source boards themselves are still not committed. KNOWN ARTWORK ISSUE: app icon PNG has opaque white corners (no alpha), so Dock/Finder may show a white square edge; needs a native transparent-corner render before release. Visual UNVERIFIED. Merged to master locally.
- 2026-09-18: Appearance (83c617b): AppearanceMode auto/light/dark persisted in SettingsStore ("appearanceMode.v1"), applied via NSApp.appearance (auto = nil = follow OS); General pane has an Appearance section (three preview tiles) first, then App, then Menu bar; About sheet adapts (dark palette + dark LogoLockup variant generated by a throwaway Swift script; source lockup PNG has a semi-opaque near-white haze, alpha ~0.5, so the light lockup may show a faint pale box on the cream background: possible polish by rebuilding its alpha the same way). Overlay stays dark on purpose. App tests: 32; Core: 73. Visual UNVERIFIED. Merged to master locally.
- 2026-09-18: design critique fully implemented (7745cf5, f1d6f46): CalendarInfo.accountName (from EventKit source title; key unchanged); Calendars pane is a table grouped by account with pinned column headers (Tug / Show in list), high-contrast keyboard-operable checkboxes with per-row VoiceOver labels; lead time is a picker (At start, 1, 2, 3, 5, 10, 15, 20, 30 min, plus any odd saved value); "Require a video link" with orange warning; "Menu bar text" label; General order App / Menu bar / Appearance. Core 76 tests, app 46 tests. Visual UNVERIFIED: watch header/row column alignment, picker+caption in the grouped form, search highlight scroll to off-screen calendar, checkbox focus ring / VoiceOver. Merged to master locally. Open ideas: rebuild light lockup alpha (haze), app icon with transparent corners, meeting-soon colored menu bar icon, mascot on overlay.
- 2026-09-18: fixed calendar header/checkbox misalignment (2d8790d): header pinned inside the ScrollView so it shares width with rows (legacy always-visible scrollbars shifted rows ~17pt). Verified by off-screen render (NSHostingView + bitmapImageRepForCachingDisplay written to PNG and viewed); this technique works for verifying SwiftUI layout without launching the GUI (it does not paint scroller knobs). Merged to master locally.
- 2026-09-18: info lines (lead-time caption, video-link warning) now share a row with their control (no divider), dimmer; sidebar and search-result icons are colored rounded squares (PaneIcon; General gray, Calendars blue, Tug Rules orange), row text primary color (4bdea4a). Off-screen render of selected sidebar row is black (render artifact; unverified in real app). Merged to master locally.
- 2026-09-18: About moved out of General into the status item right-click menu (0ceb1dd): menu is About TimeTug / separator / Settings… (cmd-,) / separator / Quit TimeTug (cmd-q); About is its own AboutWindowController window (follows NSApp.appearance), AboutView takes onDone; About search item removed. Menu order covered by StatusMenuTests (no RED-first evidence captured). Core 76 tests, app 48. Unverified in the real app: window activation from menu, real right-click menu. Render shows the faint haze box behind the light lockup (asset alpha ~0.5): still worth fixing.
- 2026-09-18: assets (bd88beb, 1c8daf7): light lockup haze removed (alpha from distance-to-white, glyph colors kept); app icon now a transparent-corner macOS-style icon (824pt rounded square centered on 1024 canvas, soft shadow; source kept as artwork/Branding/timetug-app-icon-1024-source.png; orange sparks slightly trimmed at top right; still an upscaled source, a native render would be sharper). PENDING USER-REQUESTED WORK: colored menu bar icon for meeting soon; Settings/About windows should open on the screen of the clicked icon, in the current Space, over full-screen apps and in front of Xcode (user reports windows appear behind Xcode when Xcode is on the same screen); user reported unspecified About issues (message was cut off; ask for details/screenshot); a special design session for the calendar popup and takeover screen is planned. Merged to master locally.
- 2026-09-18: colored menu bar icon (507182a): MenuBarIconState.resolve uses Scheduler.next (pending takeover starting within 10 min or in progress and unacknowledged => MenuBarColor icon, else template); StatusItemController.setIconState; 13 new tests (app total 61). Unverified: real menu bar rendering. Merged to master locally.
- 2026-09-18: window presentation fix (8b20047, 0abbd33): WindowPresenter (moveToActiveSpace + fullScreenAuxiliary, center on the display the user clicked only if the window is not already on it, NSApp.activate() plus brief .floating level then .normal); StatusItemController.clickedScreen from mouse location at click; tests 68 (app), 76 (Core). UNVERIFIED (needs the user): stacking in front of Xcode on the same display, Xcode full-screen Space, multi-display click detection. Merged to master locally. Still pending: user's unspecified About issues; special session for calendar popup + takeover screen.
- 2026-09-18: USER CONFIRMED the window presentation fix and the rest of the recent settings/About/icon work behave correctly in the real app (stacking, display placement). Next: special design session for the calendar popup (left-click dropdown) and the takeover overlay; About issues still unspecified.
- 2026-09-18: popup redesign (96f773d CalendarInfo.colorHex from EventKit; 8107d82 popup): 360pt content-sized popup (max 520, list scrolls under a fixed header), header with puppy + weekday + N meetings left + gear, solid event cards with real calendar color bars, Now pill + progress on the current meeting, next meeting hero card with orange countdown chip and provider-labelled Join button (closes popup), empty states with puppy, quiet footer showing lead time; native translucent popover background (PopupStyle.usesSolidBackground = false is a one-line switch to a solid brand surface: user wants to compare). Verified only by off-screen renders (light/dark). UNVERIFIED: real NSPopover material, live resizing, Join opening the link and closing the popup, VoiceOver. Core 82 tests, app 83. Merged to master locally. Next: takeover screen design session.
- 2026-09-18: popup cards restyle (7c094b8): PopupCardStyle glass (Liquid Glass via glassEffect/GlassEffectContainer on macOS 26+, default there) / frosted (ultraThinMaterial) / solid; live picker Settings > General > Appearance > Popup cards; Reduce Transparency and Increase Contrast fall back to solid; 20pt continuous corners, 8pt rhythm, no glass-on-glass (Join stays a solid blue capsule). Glass style UNVERIFIED visually (cannot render off-screen); user is judging by eye. App 93 tests, Core 82. Merged to master locally. Next: user-definable global hotkey to toggle the popup (KeyboardShortcuts package, no default shortcut).
- 2026-09-18: global shortcut (cd7a99a): KeyboardShortcuts 2.4.0 (exact pin in project.yml; ADR 0005); Settings > General > Shortcut recorder (no default); KeyboardShortcuts.onKeyUp(.togglePopup) calls StatusItemController.togglePopover() (activates the app, shows popover under the status item, makes it key). App 95 tests, Core 82. UNVERIFIED: real hotkey, recorder UI, popup focus, reserved-combo warning. Merged to master locally. Pending: takeover screen design session; user's unspecified About issues; user to judge the Glass popup cards on screen.
- 2026-09-18: next meeting now uses the same compact right-aligned Join button as the meeting in progress (provider name in tooltip and VoiceOver hint) instead of a full-width provider-labelled bar. Merged to master locally.
- 2026-09-18: duplicate takeover fix (26e7704, ADR 0006): investigation found 5 causes (in-memory-only ledger reset on every relaunch [user says it happened right after launch], no fire-time re-validation, fragile eventIdentifier-based identity, second takeover replacing a visible one, in-progress meetings firing on cold launch). Fixes: TakeoverLedger is Codable, dual-key (id + contentKey), persisted by LedgerStore at ~/Library/Application Support/TimeTug/takeover-ledger.json, pruned at launch and every refresh (ended, 7-day TTL, 2000-entry cap); TakeoverGuard re-checks at fire time (notInSnapshot, ended, noLongerQualifies, alreadyFired, overlayVisible queue-not-drop); meetings that started >120s before launch are acknowledged on the first refresh; every decision logged via os.Logger (subsystem com.timetug.app, category takeover; titles private). Core 106 tests, app 102. UNVERIFIED in the real app: relaunch behavior, log output, EventKit id changes. Also committed Xcode's empty 3x asset slots separately. Merged to master locally.
- 2026-09-18: takeover screen redesign (8d4ce31): blurred navy backdrop (NSVisualEffectView + tint; solid under Reduce Transparency), puppy hero (AboutHero), big countdown numeral (calm >60s, orange <=60s with pulse unless Reduce Motion, 'Starting now', red-orange 'Started N min ago'), title + details line (time range, calendar, N people), big Join capsule, Snooze menu (1/5/10), quiet Dismiss, keyboard Return/Esc/1/5/0 with hint line, VoiceOver announcement + initial focus, secondary displays mirror puppy mark + countdown + title; pure TakeoverText logic (16 tests). App 118 tests, Core 106. UNVERIFIED (needs user, Test tug on two displays): real blur, fade/pulse, focus and VoiceOver, shortcuts in a live window, Snooze menu look (tiny edge artifact on the capsule in the render), multi-display. Possible tweak: content looks small on 1920 wide displays (scale up). Merged to master locally.
- 2026-09-18: USER CLOSED the About issue (it was the window-behind-Xcode problem, fixed and confirmed). Open items now: user to run docs/manual-tests/macos-checklist.md (popup glass look, hotkey, takeover on two displays, relaunch/no-duplicate takeover, meeting moved later), choose artwork license, push to GitHub when ready (no remote yet), native icon render before release. Roadmap: sub-project 2 (public CalendarSource plugin interface), sub-project 3 (Google/iCloud CalDAV/Exchange sources with app-owned config UI and Keychain credentials).
- 2026-09-18: GitHub setup prepared (4bbc021, 6360b4e, 7deceb0): CI (core, app, core-linux allowed to fail) on macos-26, tag-triggered release workflow that publishes an UNSIGNED prerelease zip until Apple signing secrets are added (then signs with Developer ID, notarizes, staples, DMG), scripts in scripts/ci and scripts/release, community files, artwork all-rights-reserved (artwork/LICENSE.md), code MIT, docs/release.md lists the 6 secrets to add later (MACOS_CERTIFICATE_P12_BASE64, MACOS_CERTIFICATE_PASSWORD, APPLE_TEAM_ID, NOTARY_API_KEY_ID, NOTARY_API_ISSUER_ID, NOTARY_API_KEY_P8_BASE64). Repo: github.com/darkarena1/timetug (public, default branch master). UNVERIFIED until first run: macos-26 label/Xcode, cache paths, linux job, gh release create flags, signing/notarization.

- 2026-09-18: DMG installer merged (dmgbuild, branded background, verify-dmg, CI dmg job). Verified visually in Finder. CI verified green on first run; license detected as MIT. Not pushed.

## Calendar dedup + on-device inference: execution log (plan: docs/superpowers/plans/2026-09-19-calendar-dedup-inference.md)
- Task 1 complete (b43757d): model types, any-member ledger/guard. Core 112 tests.
- Task 2 complete (05cfbbe): DuplicateRules pair rules. Core 123 tests. Open note: shared-attendee/organizer merge fires before vetoes (superseded 2026-09-19: vetoes now run first).
- Task 3 complete (f3f35b3): LessonBook. Core 130 tests.
- Task 4 complete (ff08195): DuplicateAdjudicator interface + VerdictCache. Core 135 tests.
- Task 5 complete (1b65ca3): DuplicateResolver. Core 147 tests.
- Task 6 complete (8512931): resolver-backed CalendarStore, pending verdicts, decisions, state. Core 154 tests. Follow-ups queued for final fix pass: in-flight guard for resolvePending, prune on load.
- Task 7 complete (a378067): EventKit supplies attendees, organizer, external UID.
- Task 8 complete (581b354): AppleIntelligenceInference package (prompt builder + Foundation Models adapter; adapter unverified on device). Follow-ups queued for final fix pass: non-public error logging, cancellation check.
- Task 9 complete (4dec703): inferenceEnabled setting (default off), DedupStateStore, InferenceStatusText, BetaBadge, search entry. App 127 tests.
- Task 10 complete (caf39e6): Beta toggle, status line, Forget button, coordinator wiring (GUI unverified). Final fix pass queued: detach resolvePending with in-flight guard, prune on load, skip redundant persists, non-public error log, cancellation check, AppModel comment placement.
- Task 11 complete (65c4859): merge badge (Merged with <engine> / Merged manually), Not-the-same-meeting and Merge-with actions in the popup. App 131 tests (GUI unverified).
- Fix pass complete (3891c59, 3bdbc54, 4aa6cd6): in-flight guard, prune on snapshot, sort tie-break, non-public adapter logging + cancellation, non-blocking inference from refresh, deduped saves, badge a11y. Core 157, inference 5, app 131.
- Task 12 complete: 2026-09-19: calendar dedup rules + optional on-device inference (ADR 0009). Core resolver, lessons, verdict cache, adjudicator protocol; AppleIntelligenceInference package; Beta toggle (default off); merge badge and Not-the-same-meeting / Merge actions. Docs, checklist and CI step added. Verified: Core 157, inference 5, app 131 (TEST SUCCEEDED), EventKitSource builds. UNVERIFIED: real Foundation Models quality, EventKit attendee emails, popup menu behavior, all GUI behavior (no GUI or Apple Intelligence check was run), the new CI step on a real runner.
- FINAL REVIEW found 2 critical + 4 important issues (verdict-cache pruning loop, merged event can suppress a takeover, unmerge no-op for all-day/same-calendar, attendee names as emails, Webex identity, shared-attendee-before-vetoes). Fix pass 2 in progress; branch NOT ready to merge until it lands and is re-reviewed.
